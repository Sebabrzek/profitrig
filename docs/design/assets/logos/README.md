# ProfitRig production identity

Primary and compact lockups approved by the user on 21 September 2026. These approved custom outlines supersede the earlier rejected Montserrat reconstruction and the pending-source notices.

- `mark/`: unchanged bison master and one-color exports; app-icon treatment adds a Rig Green background without changing bison geometry.
- `wordmark/`: approved custom path geometry, color/white/black/reversed SVGs and high-resolution transparent PNGs.
- `lockup/`: compact `profitrig-horizontal.svg`; primary `profitrig-horizontal-tagline.svg`; white/black/reversed versions of both, with PNGs.
- `references/`: approved historical Option 01 reference, identity rules, and production proof sheet.

Use `profitrig-horizontal-tagline-reversed.svg` for the primary lockup on Rig Green. Reversed assets use Off White and Sage; white assets are pure white. PNGs are exports of the vector masters, not screenshot enlargements.

Read `references/IDENTITY_REFERENCE.md` for clear space, minimum sizes, provenance, and application typography. No frontend implementation was performed.

## Production review adjustments

Compact SVG/PNG canvases now have balanced vertical margins (1426 × 198 SVG). Approved internal artwork is unchanged. Approved primary minimum width is 1000 CSS px / 170 mm; use compact below it. Required clear space must be provided outside visible artwork and is not built into export padding. See the reference guide for full rules and actual-size validation requirements.

The user approved the production review adjustments and larger-framed favicon on 21 September 2026. Use `mark/profitrig-favicon.svg` and `mark/profitrig-favicon-{16,32,48}.png` for browser favicons. The candidate-named files and comparison sheet are retained as review history. App icons remain unchanged. No frontend implementation was performed.
